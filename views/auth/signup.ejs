<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Signup</title>
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
    <div class="container">
        <h2>Signup</h2>

        <% if (message && message.length > 0) { %>
    <div class="alert alert-danger"><%= message[0] %></div>
<% } %>


<form action="/auth/signup" method="POST">
    <div class="form-group">
        <label for="username">Username</label>
        <input type="text" id="username" name="username" required>
    </div>
    <div class="form-group">
        <label for="password">Password</label>
        <input type="password" id="password" name="password" required>
    </div>
    <div class="form-group">
        <label>Role</label>
        <div>
            <input type="radio" id="mainAdmin" name="role" value="Main Admin" required>
            <label for="mainAdmin">Main Admin</label>
        </div>
        <div>
            <input type="radio" id="guestAdmin" name="role" value="Guest Admin" required>
            <label for="guestAdmin">Guest Admin</label>
        </div>
        <div>
            <input type="radio" id="guest" name="role" value="Guest" required>
            <label for="guest">Guest</label>
        </div>
    </div>
    <button type="submit">Signup</button>
</form>


        <p>Already have an account? <a href="/auth/login">Login</a></p>
    </div>
</body>
</html>
