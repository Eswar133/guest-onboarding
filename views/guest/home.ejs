<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home - Guest Onboarding</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #121212;
            color: #f1f1f1;
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
        }
        .navbar {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 15px 30px;
            background-color: #1e1e1e;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.7);
        }
        .navbar a {
            color: #03a9f4;
            text-decoration: none;
            margin-left: 15px;
        }
        .navbar a:hover {
            text-decoration: underline;
        }
        .container {
            background-color: #1e1e1e;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.7);
            padding: 30px;
            margin: 30px auto;
            width: 100%;
            max-width: 800px;
        }
        h1 {
            color: #4caf50;
            text-align: center;
        }
        h2 {
            margin-top: 20px;
            text-align: center;
            color: #bdbdbd;
        }
        .hotel-list {
            list-style: none;
            padding: 0;
        }
        .hotel-list li {
            background-color: #2e2e2e;
            border-radius: 5px;
            padding: 15px;
            margin: 15px 0;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.5);
        }
        .hotel-list h3 {
            color: #4caf50;
        }
        .hotel-list p {
            color: #d3d3d3;
        }
        .hotel-list img {
            margin-top: 10px;
            border: 1px solid #444;
            padding: 5px;
            border-radius: 10px;
            display: block;
            max-width: 150px;
        }
        
    </style>
</head>
<body>
    <nav class="navbar">
        <div class="navbar-brand">
            <a href="/">Guest Onboarding</a>
        </div>
        <div class="navbar-links">
            <% if (!user) { %>
                <a href="/auth/login">Login</a>
            <% } else if (user.role === 'Main Admin' || user.role === 'Guest Admin') { %>
                <a href="/auth/logout">Logout</a>
            <% } %>
        </div>
    </nav>

    <div class="container">
        <h1>Welcome to Guest Onboarding</h1>
        <h2>Available Hotels</h2>
        <% if (hotels.length === 0) { %>
            <p class="text-center">No hotels available at the moment.</p>
        <% } else { %>
            <ul class="hotel-list">
                <% hotels.forEach(hotel => { %>
                    <li>
                        <h3><%= hotel.name %></h3>
                        <p><strong>Address:</strong> <%= hotel.address %></p>
                        <% if (hotel.logo) { %>
                            <img src="/uploads/<%= hotel.logo %>" alt="Hotel Logo" style="width: 150px;">
                        <% } %>
                        <% if (hotel.qrCode) { %>
                            <p><strong>QR Code:</strong></p>
                            <img src="<%= hotel.qrCode %>" alt="QR Code" style="width: 150px;">
                        <% } else { %>
                            <p>No QR Code available.</p>
                        <% } %>
                    </li>
                <% }); %>
            </ul>            
        <% } %>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
